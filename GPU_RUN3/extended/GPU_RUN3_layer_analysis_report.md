# GPU_RUN3 — NDformer 層解析レポート

Run ID: `gpu_run3_extended_1800s_groupB_20260818`  
provenance: `layer_analysis`（層解析）

解釈は最後まで区別している（plan §6.6）。probeは情報が線形に読み出せることを、
ablationはそのブロックが必要であることを、activation interventionは因果的に影響することを、
IOLEはその層だけで適応できることを、それぞれ示す。混同しない。

## 1. RQ3 — 層ごとの情報表現（Phase 4）

> `phase4/summary.json` がこのrunディレクトリに存在しません。以下のセクションは不完全です。

## 2. RQ6 — 数式構造はどの層で形成されるか（Phase 5）

> `phase5/summary.json` がこのrunディレクトリに存在しません。以下のセクションは不完全です。

## 3. RQ4 — 層の因果的寄与（Phase 6）

> `phase6/summary.json` がこのrunディレクトリに存在しません。以下のセクションは不完全です。

## 4. RQ5/RQ7 — 層ランキングと選択的fine-tuning（Phase 7）

> `phase7/summary.json` がこのrunディレクトリに存在しません。以下のセクションは不完全です。

## 5. 最終test評価（Phase 8）

> `phase8/report.json` がこのrunディレクトリに存在しません。以下のセクションは不完全です。

## 6. RQ8 — 事前学習分布との距離（Phase 9）

> `phase9/summary.json` がこのrunディレクトリに存在しません。以下のセクションは不完全です。

