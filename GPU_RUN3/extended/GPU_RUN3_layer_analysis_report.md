# GPU_RUN3 — NDformer layer-analysis report

Run ID: `gpu_run3_extended_1800s_groupB_20260818`  
Provenance: `layer_analysis`

Interpretations are kept distinct throughout (plan section 6.6): a probe shows
information is linearly readable, ablation shows a block is required, activation
intervention shows it is causally influential, and IOLE shows it can adapt.

## 1. RQ3 — layer-wise information (Phase 4)

> `phase4/summary.json` is missing from this run directory; the section below is incomplete.

## 2. RQ6 — where formula structure forms (Phase 5)

> `phase5/summary.json` is missing from this run directory; the section below is incomplete.

## 3. RQ4 — causal layer contribution (Phase 6)

> `phase6/summary.json` is missing from this run directory; the section below is incomplete.

## 4. RQ5/RQ7 — layer ranking and selective fine-tuning (Phase 7)

> `phase7/summary.json` is missing from this run directory; the section below is incomplete.

## 5. Final held-out test (Phase 8)

> `phase8/report.json` is missing from this run directory; the section below is incomplete.

## 6. RQ8 — distance to the pretraining distribution (Phase 9)

> `phase9/summary.json` is missing from this run directory; the section below is incomplete.

