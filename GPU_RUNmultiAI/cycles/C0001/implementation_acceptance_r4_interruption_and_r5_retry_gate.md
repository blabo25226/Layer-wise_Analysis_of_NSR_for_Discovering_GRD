# C0001 v16 r4 interruption and r5 retry gate

At 2026-09-24 16:37 UTC, the r4 `--implementation-acceptance` process and its Codex exec session were no longer present. The last durable `call_log.jsonl` and `stage_cache.jsonl` writes were at 15:52:11 UTC. `audit_manifest.json` remained `status: running`; no terminal/abort manifest or process exit code was available. The r4 output contains 1,627 counted call lines and 3,157 stage-cache lines, not the required 4,080 calls. The two guard side-channel files were empty. **r4 is interrupted and is not a PASS.** Its directory must remain untouched and must not be resumed.

OS journal records GNOME user session shutdown and `session-2.scope` deactivation at 15:52:11–12 UTC, coincident with the last run write. This is strong circumstantial evidence of an external session interruption, not proof of the exact signal or process exit reason. No OOM kill was found in the checked kernel journal interval; RAM and `/tmp` space were available at diagnosis. No source or frozen-plan change is indicated by the packet. The earlier independent Claude r7 source verdict and PI 152-PASS suite remain bound to `ba3ef3d`.

PI retry decision: preserve r4 as failed/incomplete evidence, use the same immutable source and frozen CLI for **one new r5 suffix**, `--implementation-acceptance --fail-if-exists` without `--resume`. The r5 output and shared guard side-channel were absent before launch. Use a separately monitored `systemd --user` transient service to decouple the run from the Codex exec PTY; capture its terminal status and journal. `loginctl` reports `Linger=no`, so a last-session logout could still terminate that service; this limitation must be monitored rather than claimed resolved. The 18,000-second/1.2-GB frozen ceilings and all scientific gates remain unchanged. This is an operational retry, not new scientific data or authorization for the full audit.
