# C0001 preregistration v6 independent closure review

- reviewed commit: `202b6c7dd8efa38545f95685e967702087fed420`
- reviewer: independent methodological Codex subagent
- verdict: **REVISE**
- firewall: PR #4、`GPU_RUNclaude1`、historical GPU_RUN5 sealed artifacts は未参照

## Closed

- v5-R1 decision precedence
- v5-R2 oracle tri-state
- v5-R3 exact rewrite prefix and SHA fixture
- v5-R4 whole-chain claim without causal stage attribution
- v5-R7 N1 deterministic construction
- v5-R8 N1/B4/linear validity gates
- confirmatory `23,550`、D2 `2,310`、grand maximum `25,860` arithmetic

## Required corrections

### V6-1 — phase8 deny depth

`phase8` と `predictions` の直前関係ではなく、campaign-relative components中で `phase8` が `predictions` より前にあれば中間深さを問わず `predictions` 自身と子孫をdenyする。norm/real各候補へ同一規則を適用する。

### V6-2 — pair ID canonical serialization

field order、separator、escaping、scale lexical form、index base、UTF-8 bytes、代表fixtureと期待SHA256をfreezeする。B3順序、resume、duplicate detectionはこのcanonical IDだけを使う。

### V6-3 — resume identity table and environment

壊れたMarkdown tableを直す。`LANSR_TED_TIMEOUT_SEC`、`LANSR_SYMPY_TIMEOUT_SEC`、`LANSR_SYMPY_MAX_NODES`を明示値に固定し、manifest/resume比較へ含める。

### V6-4 — numeric oracle tolerance

absolute/relative tolerance、結合式、全grid点一致、finite要件を明示する。nonfiniteは`completed=false`。

### V6-5 — null denominator wording

可変分母に読める「fully diagnosticのみを分母」を削除し、「固定1,320件について、全件fully diagnosticの場合にSFN=0」と記す。
