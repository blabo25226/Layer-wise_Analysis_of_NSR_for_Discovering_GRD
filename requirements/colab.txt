# Colab setup for Phase 0
# 1. Mount Drive and clone repo
# 2. Download checkpoints to NSRS/weights/ and TPSR/symbolicregression/weights/
# 3. Run: bash scripts/setup_phase0_links.sh  (or copy checkpoints manually)
# 4. pip install -r requirements/colab.txt

-r base.txt
pysr>=1.5

# TPSR upstream pins (use only if E2E backbone is needed on Linux)
# torch==1.11.0
# See TPSR/requirements.txt
