# Gemini mirror

The canonical instructions are in `.agent/`.
For Antigravity CLI, the native project customization path is `.agents/` (plural) plus root `GEMINI.md`.
This `.gemini/` directory is retained because the project explicitly wants provider-specific mirrors and for easier
human inspection. Do not treat it as a second source of truth.
